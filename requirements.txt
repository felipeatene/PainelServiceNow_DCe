# Dependências do Painel DC-e

# ─── Autenticação e Segurança ──────────────────────────────
PyJWT==2.12.1          # JWT tokens
cryptography==41.0.7   # Criptografia para JWT
python-dotenv==1.0.0   # Variáveis de ambiente (.env)

# ─── Futuro: OAuth2 / SSO ─────────────────────────────────
# authlib==1.3.0         # Suporte a OAuth2 / OpenID Connect
# msal==1.25.0           # Microsoft Authentication Library (Azure AD)
# requests==2.31.0       # HTTP client (OAuth2 flows)
